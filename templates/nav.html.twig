{% block nav %}
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('homepagina') }}">Home</a>
            </li>
            <li>
                <a class="nav-link" href="{{ path('trainingAanbod') }}">Training aanbod</a>
            </li>
            {% if is_granted('ROLE_USER') and is_granted('ROLE_INSTRUCTEUR') == false %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('inschrijven') }}">Inschrijven op les</a>
                </li>
            {% endif %}
            {% if is_granted('ROLE_INSTRUCTEUR')and is_granted('ROLE_ADMIN') == false %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('lestoevoegen') }}">Les toevoegen</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('lessenBeheer') }}">Beheer lessen</a>
                </li>
            {% endif %}
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('trainingtoevoegen') }}">Training toevoegen</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('trainingBeheer') }}">Trainingen</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('ledenBeheer') }}">Ledenbeheer</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('toevoegenInstructeur') }}">Instructeur toevoegen</a>
                </li>
            {% endif %}
            {% if is_granted('ROLE_USER') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('profiel') }}">Profiel</a>
                </li>
            {% endif %}
            <li class="nav-item">
                <a class="nav-link" href="/gedragsregels">Gedragsregels</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/contact">Contact</a>
            </li>
        </ul>
    </nav>
{% endblock %}